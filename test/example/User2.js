/* --------------------
 * Sequelize definer
 * Tests fixtures
 * User2 model definition
 * ------------------*/

'use strict';

// Modules
const Sequelize = require('sequelize');

// Exports

module.exports = {
	fields: {
		name: Sequelize.STRING
	}
};
