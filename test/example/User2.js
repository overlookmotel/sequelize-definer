/* --------------------
 * Sequelize definer
 * Tests fixtures
 * User2 model definition
 * ------------------*/

'use strict';

const Sequelize = require('sequelize');

module.exports = {
	fields: {
		name: Sequelize.STRING
	}
};
