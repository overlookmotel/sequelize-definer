/* --------------------
 * Sequelize definer
 * Tests fixtures
 * Task2 model definition
 * ------------------*/

'use strict';

const Sequelize = require('sequelize');

module.exports = {
	fields: {
		name: Sequelize.STRING
	}
};
